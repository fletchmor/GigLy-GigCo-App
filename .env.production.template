# GigCo Production Environment Template
# Copy this to .env.production and fill in real values
# NEVER commit the actual .env.production file!

# ===================================
# DATABASE CONFIGURATION (Production)
# ===================================
# Use managed database service (AWS RDS, DigitalOcean, etc.)
DB_HOST=your-production-db-host.rds.amazonaws.com
DB_PORT=5432
DB_NAME=gigco_production
DB_USER=gigco_prod_user
DB_PASSWORD=<GENERATE_STRONG_PASSWORD>
DB_SSLMODE=require  # IMPORTANT: Force SSL in production!

# ===================================
# SERVER CONFIGURATION
# ===================================
PORT=8080
ENV=production

# ===================================
# JWT AUTHENTICATION
# ===================================
# Generate with: openssl rand -base64 64
# CRITICAL: Use a strong, unique secret!
JWT_SECRET=<GENERATE_WITH_OPENSSL_RAND_BASE64_64>
JWT_EXPIRATION=24h

# ===================================
# TEMPORAL WORKFLOW ENGINE
# ===================================
TEMPORAL_HOST=temporal-production.internal:7233

# ===================================
# CLOVER PAYMENT (PRODUCTION)
# ===================================
# Get production credentials from: https://www.clover.com/
# NEVER use sandbox credentials in production!
CLOVER_ENVIRONMENT=production
CLOVER_MERCHANT_ID=<YOUR_PRODUCTION_MERCHANT_ID>
CLOVER_ACCESS_TOKEN=<YOUR_PRODUCTION_ACCESS_TOKEN>
CLOVER_API_ACCESS_KEY=<YOUR_PRODUCTION_API_KEY>
CLOVER_WEBHOOK_SECRET=<YOUR_WEBHOOK_SECRET>
PLATFORM_FEE_PERCENT=10.0

# ===================================
# MONITORING (Required for production)
# ===================================
# Sign up at: https://sentry.io (free tier available)
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
LOG_LEVEL=info

# ===================================
# CORS (Production domains only)
# ===================================
ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# ===================================
# SECURITY
# ===================================
# Additional security settings
RATE_LIMIT_PER_SECOND=10
MAX_REQUEST_SIZE=10MB
SESSION_TIMEOUT=24h

# ===================================
# BACKUPS (If using custom backup solution)
# ===================================
BACKUP_ENABLED=true
BACKUP_S3_BUCKET=gigco-prod-backups
BACKUP_SCHEDULE=0 2 * * *  # Daily at 2 AM
